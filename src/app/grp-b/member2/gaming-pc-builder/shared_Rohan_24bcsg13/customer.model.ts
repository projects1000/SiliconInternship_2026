// ============================================================
// customer.model.ts
// Customer data model
// Emitted from CustomerDetailsComponent → GamingPcBuilderComponent
// via @Output EventEmitter (Child → Parent Communication)
// ============================================================

export interface Customer {
  name: string;
  mobile: string;
  address: string;
}
